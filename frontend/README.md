# Prep Tracker Frontend

React + Tailwind CSS dashboard for tracking DSA, Striver A2Z, job outreach, ML/AI research, behavioral prep, and weekly resume prep.

```powershell
npm install
npm run dev
```

Set `VITE_API_BASE_URL` in `.env` for local or deployed backend targets.
