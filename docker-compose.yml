services:
  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    environment:
      - VITE_API_BASE_URL=http://127.0.0.1:5000/api
    ports:
      - "8080:80"
    depends_on:
      - backend

